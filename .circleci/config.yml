version: 2.1

orbs:
  python: cimg/python:3.8.4

jobs:
  build-and-test:
    executor: python/default
    steps:
      - checkout
      - run:
          command: python setup.py test
          name: Test

workflows:
  main:
    jobs:
      - build-and-test
