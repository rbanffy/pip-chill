version: 2.1

jobs:
  build-and-test:
    docker: # executor type
      - image: cimg/python:3.8.4
    steps:
      - checkout
      - run:
          command: python setup.py test
          name: Test

workflows:
  main:
    jobs:
      - build-and-test
