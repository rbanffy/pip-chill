.. highlight:: shell

============
Installation
============


Stable release
--------------

To install PIP Chill, run this command in your terminal:

.. code-block:: console

    $ pip install pip_chill

This is the preferred method to install PIP Chill, as it will always install the most recent stable release.

Requirements
~~~~~~~~~~~~

* Python 3.10 or later
* pip (usually included with Python installations)

Supported Python Versions
~~~~~~~~~~~~~~~~~~~~~~~~~

PIP Chill supports Python 3.10, 3.11, 3.12, 3.13, and 3.14.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for PIP Chill can be downloaded from the `GitHub repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone https://github.com/rbanffy/pip-chill.git

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ pip install .

Or for development installation:

.. code-block:: console

    $ pip install -e .

.. _GitHub repo: https://github.com/rbanffy/pip-chill
